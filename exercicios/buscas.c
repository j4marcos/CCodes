#include <stdio.h>

// busca

// algoritmo da buscar seguencial 
// busca um valor indo de um por um no indice de forma crescente 

// algoritmo da busca binaria 
// busca um valor em uma lista ordenada, repartindo a lista no meio e verificando se o valor esta na direta ou na esquerda ou é o valor medio, em seguida repete o processo para a parte esquerda da lista se o valor for menor ou para a parte direita se o valor é maior. 

// ordenação 

// algoritmo da ordenação (buble sort)
// verifica se dois valores estao ordenados, se não estiver, troca a posição. e repete o processo com o segundo valor e o proximo.

// algoritmo da ordenação de seleção 
// verifica qual é o menor elemento da lista, e troca esse colocando no primeiro lugar. verifica novamente apartir do segundo elemento e troca o menor valor com o segundo lugar, e assim por diante.



int main() {
    
    return 0;
}